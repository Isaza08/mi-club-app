rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Solo usuarios con sesión iniciada pueden leer/subir archivos (fotos de perfil).
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
