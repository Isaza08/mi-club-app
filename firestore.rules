rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Por ahora: acceso abierto para desarrollo. 
    // TODO: restringir por autenticación cuando se implemente Auth.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
