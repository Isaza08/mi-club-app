rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios con sesión iniciada (Administrador o Consejero) pueden leer/escribir.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
